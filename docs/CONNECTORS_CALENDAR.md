# Calendar Connectors

## Feature Flags
- `CALDAV` (default: false)
- `GOOGLE_CAL` (default: false)
- `MS_GRAPH` (default: false)

## CalDAV (Baseline)
- Use CalDAV for sync when configured.

## Google / Microsoft (Optional)
- OAuth-based sync, optional.
