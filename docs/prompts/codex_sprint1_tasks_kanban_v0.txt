CODEX TASK — Sprint 1: Tasks/Kanban v0 (tenant-safe, READ_ONLY-safe, additive)

Repo: <REPO_ROOT>
Base branch: main
New branch: codex/feat/sprint1-tasks-kanban-v0

Ziel
- Implementiere ein minimales Task/Kanban-Modul (todo/in_progress/done) als additive Erweiterung.
- Nutze vorhandene Sicherheitsprinzipien: tenant_id-Filter, READ_ONLY-Block, PII-sicheres Eventlog.

Harte Constraints
- Keine neuen Dependencies.
- Kein Netzwerk.
- Keine destruktiven Migrations.
- Eventlog ohne PII in Payload.

Lieferumfang
1) Schema (additiv, idempotent)
2) Core-Funktionen (CRUD + Statuswechsel)
3) Web-Routen + minimal Templates
4) Tests (tenant isolation, read_only, eventlog payload hygiene)
5) Docs (kurzer Runbook-Abschnitt)

Quality Gates
python -m compileall -q .
ruff check . --fix
ruff format .
pytest -q
python -m app.devtools.triage --ci --fail-on-warnings \
  --ignore-warning-regex "(?i)(swig|deprecation|userwarning|resourcewarning|warning:)"

Commit
git checkout -b codex/feat/sprint1-tasks-kanban-v0
git add app/ tests/ docs/
git commit -m "feat(tasks): add kanban v0 with tenant-safe and read-only guardrails"
git push -u origin codex/feat/sprint1-tasks-kanban-v0
