# Projects Task Move Tenant Scope Policy

- Task move operations must remain scoped to active tenant.
- Cross-tenant source or destination identifiers are forbidden.
- Violations must fail closed and emit audit evidence.
