# CI Debug Report — 2026-03-04 10:30:42

## Scope
Repository: `theosmi33-droid/kukanilea`
Role: CI Debug Worker

## 1) Run list (latest 50)
Command requested:
```bash
gh run list --repo theosmi33-droid/kukanilea --limit 50
```

Result in this environment:
- `gh` CLI is not installed (`bash: command not found: gh`).
- Direct fallback via GitHub REST API (`curl https://api.github.com/repos/theosmi33-droid/kukanilea/actions/runs`) returned `HTTP/1.1 403 Forbidden` (network/auth restriction in this runner).

## 2) Failed/cancelled runs and root cause extraction
Command requested:
```bash
gh run view <id> --log-failed
```

Could not be executed because run IDs could not be fetched via `gh` or REST in this environment.

Fallback evidence from repository-local reports points to a recurring E2E failure class:
- `tests/e2e/test_ui_workflow.py` had a hardcoded server port `5052`.
- Existing review notes already recorded failures when this port was occupied (`port 5052 already in use`).

Root cause (cluster):
1. **Playwright E2E flakiness due to fixed port binding (`5052`)** in `tests/e2e/test_ui_workflow.py`.
2. Secondary stability issue: startup readiness used a fixed sleep (`time.sleep(3)`) instead of active health polling.

## 3) Concrete fix PR (small + reproducible)
Implemented targeted CI-only test hardening:
- Replace fixed `5052` with a dynamically reserved free localhost port.
- Replace fixed startup sleep with bounded `/login` readiness polling.

This keeps functional test behavior unchanged while removing a non-deterministic infrastructure failure mode.

## 4) Local validation
Executed in this environment:
- `python3 -m compileall tests/e2e/test_ui_workflow.py` ✅

Could not execute full pytest suite here due missing Python packaging tools in container image:
- `/usr/bin/python3: No module named pytest`
- `/usr/bin/python3(.12): No module named pip`

## 5) Pipeline re-check
Live re-check of remote pipeline status was not possible in this environment because:
- `gh` unavailable.
- GitHub REST access for actions endpoint returned `403 Forbidden`.

## Changed files
- `tests/e2e/test_ui_workflow.py`
- `docs/reviews/codex/CI_DEBUG_20260304_103042.md`
