# UI Audit Report (Codex Cloud)

## Scope
- Full template link integrity audit across `app/templates/**` using Flask route-map verification.
- High-volume HTTP navigation audit against running app (`http://127.0.0.1:5051`) to exercise UI entrypoints and discovered links.

## Before (deterministic defects)
Route-map validation identified dead links in templates:
- `app/templates/dashboard.html` → `/archive` (no matching route)
- `app/templates/dashboard/index.html` → `/archive` (no matching route)
- `app/templates/automation/insights_daily.html` → `/leads/inbox` (no matching route)

## Fixes applied
- Repointed dashboard activity "Verlauf" links from `/archive` to `/admin/audit`.
- Repointed daily insights lead KPI cards from `/leads/inbox?...` to `/automation/pending`.

## After (validation evidence)
- Template route-map validation reports `missing_count 0` (no static dead internal links).
- HTTP UI action audit completed with `action_count 2300`, `unique_paths 39`, and `server_errors []`.

## Notes
- Browser-container screenshot capture was attempted earlier but Playwright Chromium crashed in this environment (SIGSEGV), so evidence is command-based for this run.
