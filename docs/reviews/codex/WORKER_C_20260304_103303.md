# Worker C Review Report — kalender / aufgaben / zeiterfassung / projekte / einstellungen

**Timestamp:** 2026-03-04 10:33:03  
**Branch:** `work`  
**Worker:** Domain Worker C

## 1) Branch-Sync + Konfliktauflösung

### Ergebnis
- Lokaler Arbeitsbaum ist sauber (`git status --porcelain` ohne Output).
- Keine ungelösten Merge-Konflikte (`git ls-files -u` ohne Output).
- Keine `codex/*` Domain-Branches lokal vorhanden (`git branch --list 'codex/*'` ohne Output).
- Keine Git-Remotes konfiguriert (`git remote -v` ohne Output) → echter Upstream-Sync/Fetch nicht möglich.

### Bewertung
- **P1:** Branch-Sync gegen `main`/Remote aktuell **blockiert**, weil kein Remote hinterlegt ist.
- **P2:** Kein akuter Konflikt im aktuellen Branch `work`.

## 2) Overlap-Check + Tests

## Overlap-Check (pro Domain)
Ausgeführt mit `python3 scripts/dev/check_domain_overlap.py --reiter <domain> --files <domain-owned-file> --json`.

- `kalender` mit `app/knowledge/ics_source.py` → **OK**
- `aufgaben` mit `app/modules/projects/logic.py` → **OK**
- `zeiterfassung` mit `app/modules/projects/logic.py` → **OK**
- `projekte` mit `app/modules/projects/logic.py` → **OK**
- `einstellungen` mit `app/routes/admin_tenants.py` → **OK**

### Tests
Versucht:
- `pytest -q ...` → fehlgeschlagen wegen Pyenv/Python-Tooling im Environment.
- `python3 -m pytest -q ...` → fehlgeschlagen, da `pytest` für `/usr/bin/python3` nicht installiert.

### Bewertung
- **P1:** Vollständige Testvalidierung ist in dieser Session nicht möglich (fehlendes `pytest`).
- **P2:** Overlap-Policy-Checks sind auf den gewählten Domain-Pfaden grün.

## 3) Integrationspunkte prüfen

### A) Aufgaben <-> Projekte
**Status: integriert mit klaren Kopplungspunkten**

- Task-Strukturen und Event-/Notification-Logik liegen im Project-Hub-Modul (`team_tasks`, `team_task_events`, `team_task_notifications`).
- Karten besitzen `linked_task_id` und können explizit mit Tasks verknüpft werden (`link_card_task`).
- Zusätzlich kann pro Karte ein Zeiterfassungs-Timer über Callback gestartet werden (`start_timer_for_card`).

**Bewertung:**
- **P2:** Integrationspfad ist vorhanden und nachvollziehbar.
- **P2:** Potenzielles Architektur-Risiko bleibt, weil Aufgaben + Projekte aktuell stark im selben Modul (`app/modules/projects/logic.py`) konzentriert sind (Ownership sauber halten).

### B) Zeiterfassung GoBD (Storno statt Delete)
**Status: überwiegend konform, aber mit offener Lücke**

Positiv:
- Für Zeiteinträge sind Start/Stop/Edit/Approve als auditable Operationen vorhanden (`TIME_ENTRY_START`, `TIME_ENTRY_STOP`, `TIME_ENTRY_EDIT`, `TIME_ENTRY_APPROVE`).
- In `app/web.py` sind nur POST/GET-Endpunkte für Start/Stop/Edit/Approve/Export erkennbar; kein offensichtlicher Delete-Endpunkt für Time Entries.

Offene Lücke:
- Es ist kein expliziter **Storno-Workflow** (z. B. `TIME_ENTRY_STORNO`, `is_storno`, `storno_reason`, Referenz auf Originalbuchung) erkennbar.

**Bewertung:**
- **P1:** GoBD-Anforderung „Storno statt Delete“ ist praktisch unterstützt (kein Delete-Endpoint sichtbar), aber nicht als formaler Storno-Mechanismus modelliert.

### C) Einstellungen: Lizenz / Backup / Mesh
**Status: gut integriert**

- **Lizenz:** Upload mit Confirm-Gate, JSON-Validierung, Test-Validierung vor Commit, atomarer Swap und restriktive Datei-Rechte.
- **Backup:** Backup-Liste, Backup-Erstellung (`auth/core/license`) sowie Restore mit Allowlist und Confirm-Gate.
- **Mesh:** Peer-Handshake über validierte Adresse/Port, Key-Rotation mit Confirm-Gate und Neu-Initialisierung der Identität.

**Bewertung:**
- **P2:** Integrationspunkte sind vorhanden, inklusive Sicherheits-/Governance-Gates.

## Findings (P0/P1/P2)

### P0
- Keine P0-Findings in dieser Session.

### P1
1. **Fehlender Upstream verhindert echten Branch-Sync**
   - Repro: `git remote -v` liefert nichts.
   - Risiko: Divergenzen zu `main`/anderen Worktrees bleiben unerkannt.
   - Empfehlung: `origin` konfigurieren, `git fetch --all --prune`, dann Merge/Rebase-Check gegen `main` durchführen.

2. **GoBD-Storno nicht explizit modelliert**
   - Repro: kein klarer `storno`-Pfad/Status für `time_entries` erkennbar.
   - Risiko: fachliche Unschärfe bei Korrekturfällen trotz Audit-Logs.
   - Empfehlung: „Storno-Eintrag“ als explizites Datenmodell + API/UX + Audit-Event ergänzen.

3. **Tests nicht ausführbar im Environment**
   - Repro: `python3 -m pytest ...` → `No module named pytest`.
   - Risiko: fehlende Regression-Absicherung dieser Review.
   - Empfehlung: CI- oder lokale Test-Umgebung mit installierten Dev-Dependencies verwenden.

### P2
1. Keine aktuellen Merge-Konflikte im Branch `work`.
2. Domain-Overlap-Checks auf repräsentativen Domain-Pfaden waren durchgehend OK.

## Durchgeführte Fixes
- Keine Code-Fixes umgesetzt (Analyse- und Review-Durchlauf).

## Offene Blocker inkl. reproduzierbarer Schritte
1. **Remote fehlt**
   - `git remote -v`
   - Ergebnis: leer.

2. **Pytest fehlt**
   - `python3 -m pytest -q tests/test_knowledge_ics_minimal.py`
   - Ergebnis: `/usr/bin/python3: No module named pytest`.

## Empfohlene nächste Schritte
1. Git-Remote setzen und Domain-Branches mit `main` synchronisieren.
2. Expliziten GoBD-Storno-Flow für Zeiterfassung spezifizieren und implementieren.
3. Testumgebung (pytest + Projekt-Dependencies) in CI und lokal harmonisieren.
