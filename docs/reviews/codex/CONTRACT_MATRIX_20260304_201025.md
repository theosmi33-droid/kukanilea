# CONTRACT_MATRIX_20260304_201025

## Scope
- MISSION: CONTRACT_COMPLETENESS_1000.
- Validated `/api/<tool>/summary` + `/api/<tool>/health` for all 11 contract tools.
- Enforced chatbot payload standardization (`message/msg/q` in, `ok/response` out).
- Kept dashboard in strict aggregation mode (no copied domain logic).

## 11/11 Tool Contract Matrix
| Tool | `/api/<tool>/summary` | `/api/<tool>/health` | Result |
|---|---|---|---|
| dashboard | ✅ | ✅ | summary aggregates matrix-derived status only |
| upload | ✅ | ✅ | standardized contract payload |
| projects | ✅ | ✅ | standardized contract payload |
| tasks | ✅ | ✅ | standardized contract payload |
| messenger | ✅ | ✅ | standardized contract payload |
| email | ✅ | ✅ | standardized contract payload |
| calendar | ✅ | ✅ | standardized contract payload |
| time | ✅ | ✅ | standardized contract payload |
| visualizer | ✅ | ✅ | standardized contract payload |
| settings | ✅ | ✅ | standardized contract payload |
| chatbot | ✅ | ✅ | payload aliases + response normalization declared |

## Dashboard / Chatbot Standardization
- Dashboard summary now aggregates status/health from the tool-contract matrix without embedding domain logic.
- Chat endpoints now share one canonical request parser (`message/msg/q`) and one response normalizer (`ok/text/response` compatibility).
- Chatbot summary contract explicitly advertises the same payload shape used at runtime.

## Validation Gates
- `pytest -q tests/contracts`
- `pytest -q tests/integration/test_dashboard_tool_matrix.py`
- `./scripts/ops/healthcheck.sh`

## Notes
- In this container, Python dependency bootstrap is blocked by proxy (`403`) for `pip install -r requirements-dev.txt`; gate execution is documented in the ledger.
