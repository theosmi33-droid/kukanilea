# Domain Worker C Status Report — 2026-03-03

## Overview
Worker C has evaluated the domains: **Kalender, Aufgaben, Zeiterfassung, Projekte, Einstellungen**.
All worktrees have been synced with the latest `main` (post-PR #203).

## 1. Domain Status & DoD
| Domain | Status | Overlap | DoD Status |
|---|---|---|---|
| **Kalender** | PARTIAL | CLEAN | Technical core (ICS) ready, UI missing. |
| **Aufgaben** | READY | CLEAN | Integrated with Projects. Tests pass. |
| **Zeiterfassung** | READY | CLEAN | Core-heavy logic. Tests pass. |
| **Projekte** | READY | CLEAN | Kanban & Hub logic stable. Tests pass. |
| **Einstellungen** | DONE | CLEAN | Hardening complete (PR #203). |

## 2. Integration Edges (Analysis)
- **Task <-> Project**: Strong integration via `ProjectManager`. Tasks can be linked to Project Cards.
- **Project <-> Time**: Integration exists via `start_timer_for_card` which triggers the `zeiterfassung` engine.
- **Task/Project <-> Calendar**: 
  - *Current*: Manual (ICS parsing into knowledge chunks).
  - *Gaps*: No automatic sync of `due_at` from tasks into the calendar feed yet. (Priority P2).

## 3. Priority Issues (P0-P2)
- **P0 (Critical)**: Finalize dedicated files for `floating-widget-chatbot` (Domain Worker B/C crossover).
- **P1 (High)**: Create dedicated `app/modules/time/` to move logic out of `app/core/logic.py` (Domain Cleanliness).
- **P2 (Medium)**: Implement automatic task deadline export to ICS in `kalender` domain.

## 4. Next Merge Wave Order
To minimize conflicts and maintain stability, the following merge order is recommended:
1.  **codex/aufgaben** (Since it's stable and clean).
2.  **codex/projekte** (Integrated with Aufgaben).
3.  **codex/zeiterfassung** (Relies on core tables, merge after tasks/projects).
4.  **codex/kalender** (Lacks UI, merge last as it's primarily knowledge-engine based).

## 5. Verification Results
- **Overlap Check**: `status=OK` for all domains (after Worker C sync).
- **Pytest**: All relevant tests passed (10/10 across domains).

---
*Report generated by Domain Worker C.*
