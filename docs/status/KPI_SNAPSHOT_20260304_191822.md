# KPI Snapshot — PERF_STRESS_1000 (2026-03-04 19:18:22 UTC)

## Pflicht-Gates (Start)
- `bash scripts/dev/vscode_guardrails.sh --check`: **OK** (Interpreter-Warnung auf fehlende `.build_venv` wurde toleriert).
- `bash scripts/orchestration/overlap_matrix_11.sh`: **OK**.
- `./scripts/ops/healthcheck.sh`: **nicht vollständig** (Abbruch, da `pytest` in Runtime nicht installiert).
- `scripts/ops/launch_evidence_gate.sh`: **fehlerhaft** (`fatal: Needed a single revision`).

## Benchmarks / Stress / Last
- `python3 scripts/tests/perf_insights_benchmark.py`
  - Legacy Mean: **4.503 ms**
  - Optimized Mean: **3.713 ms**
  - Verbesserung: **17.54%**
- `python3 scripts/tests/stress_test_fast.py`
  - Abbruch: `ModuleNotFoundError: No module named 'flask'`
- `python3 scripts/tests/playwright_benchmark.py`
  - Script modernisiert (Playwright-Flow + HTTP-Fallback), Lauf scheitert aktuell wegen nicht gestartetem lokalen Server (`127.0.0.1:5051`).

## Top-Engpässe
1. **DB (insights aggregation)**
   - Vorher: 4 Einzel-COUNT-Queries auf `leads` (zusätzlich zu weiteren Queries).
   - Nachher: 1 aggregierte Query mit `SUM(CASE WHEN ...)` für die vier Lead-Metriken.
   - Messbar: -17.54% Mean-Latenz im synthetischen Benchmark.
2. **Routing**
   - Reale Route-Latenzen konnten ohne laufenden lokalen Webserver nicht vollständig reproduziert werden.
   - Benchmark-Script für Routing-Messung wurde vorbereitet (`playwright_benchmark.py`).
3. **Rendering/UI**
   - E2E/Playwright-Rendering nicht messbar ohne laufenden App-Server + Browser-Dependencies.
   - KPI-Harness ist vorhanden und schreibt Report nach `docs/status/playwright_benchmark_latest.json` (wenn Server läuft).

## Implementierte Optimierungen
- `app/modules/automation/insights.py`
  - Lead-KPI-Berechnung von 4 getrennten SELECTs auf 1 aggregierte Query reduziert.
  - Rückgabe-Mapping auf aggregierte Felder umgestellt.
- `scripts/tests/playwright_benchmark.py`
  - Von Stub auf nutzbaren Benchmark-Harness erweitert:
    - Playwright-Navigation `/login` + `/`
    - API-Messung `/api/health`
    - Fallback ohne Playwright via HTTP
    - KPI-Report-JSON
- `scripts/tests/perf_insights_benchmark.py`
  - Neuer reproduzierbarer Vorher/Nachher-Benchmark für die Insights-DB-Query.

## Action Ledger
- Datei: `docs/reviews/codex/ACTION_LEDGER_PERF_STRESS_20260304_1918.md`
- Enthält **1000 nummerierte, ausgeführte SQLite-Perf-Aktionen** mit Zeitstempel und Latenz.
