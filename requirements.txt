# Production/runtime dependencies (pinned for reproducible installs).
# Upgrade strategy:
# 1) Update one package at a time via `python -m pip install --upgrade <package>`.
# 2) Run `python -m pip freeze > requirements.lock` (or your lock refresh workflow).
# 3) Copy the reviewed direct dependency version into this file and run CI.
Flask==3.1.3
requests==2.32.5
python-dotenv==1.2.2
pydantic==2.12.5
rapidfuzz==3.14.3
pytest==9.0.2
waitress==3.0.2
platformdirs==4.5.1
cryptography==46.0.5
psutil==7.2.2
PyMuPDF==1.27.1
PyNaCl==1.6.2
pyinstaller==6.19.0
dmgbuild==1.6.7
pip-audit==2.10.0
yake==0.7.3
