name: KUKANILEA CI/CD Pipeline

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  delegated-ci:
    name: Delegated CI (single source of truth)
    uses: ./.github/workflows/ci.yml
    with:
      run_perf_gate: false
