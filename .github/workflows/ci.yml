name: KUKANILEA CI

on:
  push:
    branches: [main, "codex/**"]
  pull_request:
    branches: [main, "codex/**"]
  schedule:
    - cron: "13 4 * * *"
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    env:
      OLLAMA_ENABLED: "0"
      PYTHONUNBUFFERED: "1"
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: "pip"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt -r requirements-dev.txt

      - name: Validate VS Code config policy
        run: |
          python scripts/dev/enforce_vscode_configs.py --check

      - name: Run release healthcheck (CI aligned)
        run: |
          bash scripts/ops/healthcheck.sh --ci

      - name: Run tests (without e2e)
        run: |
          pytest -q tests --ignore=tests/e2e
