[pytest]
addopts = --durations=10
markers =
    slow: slow tests
    integration: integration tests
    unit: unit tests
    smoke: fast confidence tests
    standard: default CI-safe regression profile
    full: exhaustive tests including long-running suites
    external: test intentionally uses external services/network (opt-in)
testpaths = tests
