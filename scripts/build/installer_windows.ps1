Set-StrictMode -Version Latest
$ErrorActionPreference = "Stop"

Write-Host "Windows installer packaging placeholder."
Write-Host "Current Phase 5.1 output is a standalone EXE from bundle_windows.ps1."
Write-Host "Future step: generate NSIS/MSI installer and optional code signing."
